<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.maxdexter.mvpmoxy">
    <uses-permission android:name="android.permission.INTERNET"/>
    <application
        android:name=".test.unit.LeakCanaryClass"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".test.unit.TextActivity">

        </activity>
        <activity android:name=".di.dagger.DaggerActivity">

        </activity>
        <activity android:name=".di.diActivity.DIActivity"></activity>
        <activity android:name=".di.nodi.NoDiActivity" />
        <activity android:name=".room.RoomActivity" />
        <activity android:name=".retrofit.RetrofitActivity" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>